name: Keep Azure Backend Awake

on:
  schedule:
    # Runs every 15 minutes
    - cron: '*/15 * * * *'
  workflow_dispatch: # Allows you to trigger it manually if needed

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit Health Endpoint
        run: curl -I https://teamtaskmanagerethara-fnhmeedjd7dfd0h4.westindia-01.azurewebsites.net/actuator/health